apiVersion: apps/v1
kind: Deployment
metadata:
  name: keycloak
  namespace: nethub
  labels:
    app: keycloak
    app.kubernetes.io/name: keycloak
    app.kubernetes.io/part-of: nethub
    nethub.co.ke/migrated-from: nethub-cluster
spec:
  replicas: 1
  selector:
    matchLabels:
      app: keycloak
  template:
    metadata:
      labels:
        app: keycloak
        app.kubernetes.io/name: keycloak
    spec:
      imagePullSecrets:
        - name: ghcr-registry-key
      containers:
        - name: keycloak
          # Built from NetHub-Ltd/keycloak-config (themes + realm templates on 26.7.3)
          image: ghcr.io/nethub-ltd/keycloak:0.0.3 # {"$imagepolicy": "flux-system:keycloak"}
          imagePullPolicy: IfNotPresent
          args: ["start"]
          ports:
            - name: http
              containerPort: 8080
            - name: management
              containerPort: 9000
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "1000m"
              memory: "2Gi"
          envFrom:
            - configMapRef:
                name: keycloak-config
            - secretRef:
                name: keycloak-secrets
          startupProbe:
            httpGet:
              path: /health/started
              port: management
            failureThreshold: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: management
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health/ready
              port: management
            periodSeconds: 10
