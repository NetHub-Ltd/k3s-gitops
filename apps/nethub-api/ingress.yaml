apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nethub-api-ingress
  namespace: nethub
  labels:
    app.kubernetes.io/name: nethub-api
  annotations:
    kubernetes.io/ingress.className: traefik
    traefik.ingress.kubernetes.io/service.passhostheader: "true"
    traefik.ingress.kubernetes.io/custom-response-headers: "Cache-Control: public, max-age=31536000"
spec:
  rules:
    - host: api.nethub.co.ke
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nethub-api-service
                port:
                  number: 80
